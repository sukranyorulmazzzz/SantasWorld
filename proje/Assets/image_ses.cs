using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class image_ses : MonoBehaviour
{
    public AudioSource imageSound;

    public void resim_Sound()
    {
        imageSound.Play();
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
